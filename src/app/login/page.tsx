import { login, register } from '@/app/actions';

export default async function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) {
  const { error } = await searchParams;
  return <main className="auth-page"><section className="auth-panel"><div className="brand-mark">DL</div><p className="eyebrow">DAILY LEDGER</p><h1>A clearer view of your money.</h1><p className="auth-copy">Keep every day accountable, from the first rupee to the closing balance.</p><form action={login} className="auth-form"><label>Email<input name="email" type="email" placeholder="you@example.com" required /></label><label>Password<input name="password" type="password" placeholder="8+ characters" required /></label>{error && <p className="error-message">{decodeURIComponent(error)}</p>}<button className="primary-button full" type="submit">Sign in <span>→</span></button></form><p className="signup-note">New here? Create an account below.</p><form action={register} className="auth-form signup-form"><label>Email<input name="email" type="email" placeholder="you@example.com" required /></label><label>Password<input name="password" type="password" placeholder="8+ characters" minLength={8} required /></label><button className="secondary-button full" type="submit">Create account</button></form></section><aside className="auth-aside"><div className="aside-top"><span>09 / 08 / 2026</span><span>PRIVATE LEDGER</span></div><div className="quote"><span className="quote-mark">“</span><p>Small entries make a trustworthy picture.</p><span className="quote-by">Your daily record, kept simple.</span></div><div className="aside-bottom"><span>Secure by design</span><span>⌁</span><span>Built for consistency</span></div></aside></main>;
}
